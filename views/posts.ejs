<div class="single-post">
    <div class="single-post-content">
        
        <h2> <%= data.title %> </h2>
        <img loading="lazy" src="https://images.pexels.com/photos/263402/pexels-photo-263402.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1" alt="image slider">
        <p> <%= data.description %> </p>

        <div class="post-footer">
            <span class="tag-single"> <%= data.category %> </span>
            <span> <%= getTimeDifference(data.createdAt) %> </span>
        </div>
    </div>

    <div class="related-post">
        <h4>Related Posts</h4>
        <ul>
            <% if (relatedPosts && relatedPosts.length > 0) { %>
                <% relatedPosts.forEach(post => { %>
                    <li> <a href="/post/<%= post._id %>"><%= post.title %></a> </li>
                <% }) %>
            <% } else if (relatedPosts) { %>
                <li> <a href="/post/<%= post._id %>"><%= relatedPosts.title %></a> </li>
            <% } else { %>
                <li> No Post Found </li>
            <% } %>
        </ul>
    </div>
</div>
